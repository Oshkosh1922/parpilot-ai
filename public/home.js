const money = (value) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(value);
const form = document.querySelector("#roi-form");
async function updateRoi(event) { event?.preventDefault(); const annualSales = Number(document.querySelector("#annual-sales").value || 0); try { const response = await fetch("/api/roi", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ annualSales }) }); if (!response.ok) throw new Error("ROI model unavailable"); const result = await response.json(); document.querySelector("#roi-food").textContent = money(result.foodSavings); document.querySelector("#roi-labor").textContent = money(result.laborSavings); document.querySelector("#roi-guest").textContent = money(result.guestGrowth); document.querySelector("#roi-total").textContent = money(result.total); } catch { const food = annualSales * 0.015, labor = annualSales * 0.009, guest = annualSales * 0.012; document.querySelector("#roi-food").textContent = money(food); document.querySelector("#roi-labor").textContent = money(labor); document.querySelector("#roi-guest").textContent = money(guest); document.querySelector("#roi-total").textContent = money(food + labor + guest); } }
form.addEventListener("submit", updateRoi); document.querySelector("#annual-sales").addEventListener("change", updateRoi); updateRoi(); if ("serviceWorker" in navigator) navigator.serviceWorker.register("/sw.js").catch(() => {});
